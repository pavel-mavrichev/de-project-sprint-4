1. Описание структур данных в api_entities.md. Кроме дополнительных таблиц для слоя stg (курьеры и доставки), 
    но они ровно такие же как и для данных, извлекаемых из MongoDB. Также они есть в скриптах инициализации 
    схем в "/lessons/dags/examples/stg/init_schema_dag/ddl"
2. Изначальная структура скопирована из занятия (не целиком, так как пришлось восстанавливать), поэтому DAG'ов 
    несколько. Вообще, наверно, можно было бы и в 1 объединить.
3. В настройках самого Airflow для корректного исполнения помимо соединений с PG и переменных для подключения
    к MongoDB необходимо добавить одну переменную
    DELIVERY_API_ENDPOINT = "https://d5d04q7d963eapoepsqr.apigw.yandexcloud.net" остальное определяется в коде
4. Витрина dm_courier_ledger пересобирается каждый раз с начала прошлого месяца до текущей даты, так как в 
    задании написано, что необходимо рассчитать суммы оплаты каждому курьеру за предыдущий месяц. Но при этом 
    дальше указано, что грузить надо только последние 7 дней - в таком случае, если расчитывать только 
    предыдущий месяц, то данных бы никаких в витрину не попадало.
